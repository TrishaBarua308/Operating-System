#find greatest
echo "Input 3 numbers"
read a 
read b 
read c 
if [ $a -gt $b ];
then
    if [ $a -gt $c ];
    then   echo "$a larger"
    else
           echo "$c larger"
    fi
else
    if [ $b -gt $c ];
    then   echo "$b larger"
    else   echo "$c larger"
    fi
fi
